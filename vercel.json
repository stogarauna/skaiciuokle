{
  "framework": "sveltekit",
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel/output",
  "devCommand": "npm run dev"
}{
  "version": 2,
  "builds": [{ "src": "package.json", "use": "@sveltejs/adapter-vercel" }],
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
